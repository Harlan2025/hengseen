# 🔧 修复 AI 响应 JSON 解析问题

## 问题原因
Agnes AI 返回的是 markdown 代码块格式：
```
```json
{
    "text": "...",
    "category": "...",
    "required": true
}
```
```

但代码只处理了开头的 ` ``` `，没有正确处理结尾。

---

## 修复内容
修改 `routers/interview.py` 中的 JSON 解析逻辑：
1. 去除开头的 ` ```json ` 或 ` ``` `
2. 去除结尾的 ` ``` `
3. 然后解析 JSON

---

## 已完成的修复
✅ 已修复 AI 响应解析逻辑

---

## 下一步
1. 重启后端服务
2. 测试访谈功能
3. 确认 AI 生成问题正常
