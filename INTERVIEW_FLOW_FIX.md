# 🔧 访谈流程问题修复

## 问题诊断

### 现象
点击"开始访谈"后，提交一个答案就显示访谈完成，而不是继续提问。

### 根本原因
1. **前端硬编码 totalSteps=5**
   - 后端 `calculate_total_questions()` 返回20
   - 前端硬编码为5，导致5个步骤后就认为完成

2. **前端检查错误的完成条件**
   - 前端检查 `res.data.next_question`
   - 但后端没有返回此字段
   - 后端返回的是 `step` 和 `total_steps`

3. **后端总问题数计算不合理**
   - 当前硬编码返回20个问题
   - 应根据项目类型动态计算

---

## 已完成的修复

### 1. 修复前端提交逻辑
- 使用后端返回的 `step` 和 `total_steps`
- 检查 `project_status === 'outline_generated'` 判断完成

### 2. 更新前端默认值
- `totalSteps` 从5改为20（与后端保持一致）

---

## 待完善的后续优化

### 1. 后端动态计算总问题数
根据项目类型（主类型+附属类型）动态计算合理的访谈问题数量：
- 单一主类型：约10-15个问题
- 主类型+1个附属：约15-20个问题
- 主类型+2个附属：约20-25个问题

### 2. 后端生成更有意义的访谈问题
当前问题只是模板字符串，需要实现真正的AI生成逻辑。

### 3. 后端解析用户回答并提取要素
需要实现AI解析逻辑，从用户回答中提取：
- confirmed_elements（已确认要素）
- pending_elements（待确认要素）
- risks（风险提示）

---

## 下一步
重新构建并部署前端，测试访谈流程是否正常。
